# ─── Convex ───────────────────────────────────────────────────────────────────
# Run `bunx convex dev` in packages/backend to get these values.

CONVEX_DEPLOYMENT=dev:your-deployment-name

# Frontend URLs — copy from the Convex dashboard after deploying
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
EXPO_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# ─── Managed by Bloom (do not edit manually) ─────────────────────────────────
# These are set automatically by Bloom. You can set them locally for testing.
# BLOOM_PROJECT_ENV=preview
# SITE_URL=https://your-deployment.convex.site
